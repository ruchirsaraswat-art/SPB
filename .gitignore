# Python
backend/.venv/
__pycache__/
*.pyc

# Node / frontend build
node_modules/
frontend/dist/

# Downloaded toolchain binaries (97MB; see audits/2026-08-20-behavioral-models-spec.md for install)
tools/

# Generated run outputs (kept local; filed results live in libraries/)
runs/
research_runs/
fw_runs/

# Chat transcripts (personal working notes, not project source)
arch_chats/
if_chats/
fw_chats/

# User-attached specification documents (may be copyrighted, e.g. JEDEC PDFs —
# never publish; repo is public)
spec_docs/

# Imported DDR channel models + their fit artifacts (user-supplied Touchstone
# files may be confidential; the fits are large and reproducible from source)
channels/

# Local scratch
*.log
.DS_Store

# Compiled host-test binaries
firmware/**/test_*
!firmware/**/test_*.c
